def gas_stations(distance, tank_size, stations):
	stations.append(distance)
	if stations[0] > tank_size:
		break

	result = []

	index for index in range(0, len(stations)):
		stations[index] - tank_size
		if stations[index] > 0:
			reuslt.append(stations[index - 1])